namespace LunaPanel.Core;

/// <summary>
/// Identifies this application build. The server's health/status surface
/// (and anything else that needs to say which product is running) reads
/// this rather than hard-coding the name in more than one place.
/// </summary>
public static class AppInfo
{
    public const string Name = "LunaPanel";
}
